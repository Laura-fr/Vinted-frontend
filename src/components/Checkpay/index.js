import React from "react";
// import { useStripe, useElements, CardElement } from "@stripe/react-stripe-js";
// import axios from "axios";

const Checkpay = ({ price }) => {
  return (
    <div>
      <form></form>
    </div>
  );
};

export default Checkpay;
